
	<% @posts.each do |post|%>
	<div class="col-xs-10 col-xs-offset-1 simple-box">
		<h2><%= post.title %></h2>
		<h3><%= post.body %></h3>
		<small><%= post.created_at %></small>
		</div>
		<div class="white-box col-xs-12">
		<br />
		<br />
		</div>
		

	<% end %>

	

